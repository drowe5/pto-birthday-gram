# Keep the key out of the web. config.php is read by cheddar.php from disk and
# must never be served, whether or not PHP happens to be running.
<FilesMatch "^(config\.php|config\.sample\.php)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
